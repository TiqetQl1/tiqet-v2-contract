{
  "name": "tiqet-v2",
  "scripts": {
    "ignite": "npx hardhat ignition deploy ignition/modules/%npm_config_file%.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.23.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0"
  }
}
